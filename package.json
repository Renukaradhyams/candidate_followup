{
  "name": "bsc-hrms-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd hrms-system/client && npm install && npm run build",
    "start": "cd hrms-system/client && npm start",
    "server": "cd hrms-system/server && npm start"
  }
}
