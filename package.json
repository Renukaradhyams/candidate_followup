{
  "name": "bsc-hrms-unified",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd hrms-system/client && npm install && npm run build",
    "start": "node server.js",
    "seed": "cd hrms-system/server && npm run seed"
  }
}
